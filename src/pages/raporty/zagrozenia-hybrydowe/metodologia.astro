---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { methodology } from '../../../lib/reports';
import { renderMarkdown } from '../../../lib/markdown';
const entry = methodology(); if (!entry) throw new Error('Missing methodology');
---
<BaseLayout title="Metodologia ocen"><div set:html={renderMarkdown(entry.body)} /></BaseLayout>
